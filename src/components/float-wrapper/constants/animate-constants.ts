/**
 * Default spring animation options used for all anchor snap transitions.
 *
 * @see https://motion.dev/docs/animation#spring
 */
export const defaultAnimate = {
  type: "spring",
  stiffness: 300,
  damping: 30,
} as const;
